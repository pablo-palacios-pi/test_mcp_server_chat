version: '3.8'
services:
  mcp_server:
    build:
      context: .
      dockerfile: Dockerfile.mcp
    ports:
      - "8000:8000"

  backend:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "7100:7100"
    depends_on:
      - mcp_server
